#!/bin/bash

# set up git identity on personal PC
git config --global user.email "ghosalatreyee@gmail.com"
git config --global user.name "AdLucem"

# set up git credentials on personal PC
git config --global credential.helper store

git pull
